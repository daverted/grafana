
module.exports = function(grunt) {
  "use strict";

  // This is no longer building the frontend properly; use yarn start instead. -ds
  // Concat and Minify the src directory into dist
  grunt.registerTask('build', [
    // 'clean:release',
    // 'clean:build',
    // 'phantomjs',
    // 'exec:webpack',
  ]);

};
